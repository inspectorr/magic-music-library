{
  "name": "magic-music-library",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "secure": "chmod +x secure.sh && ./secure.sh",
    "deploy": "docker-compose build && docker-compose up --detach",
    "deploy-verbose": "yarn build-app && docker-compose build && docker-compose up"
  }
}
