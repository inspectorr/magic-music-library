{
  "name": "magic-music-library",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev:api": "cd api && yarn dev",
    "deploy:production:api": "git subtree push --prefix api heroku master && yarn production:logs",
    "production:logs": "heroku logs --tail",
    "empty": "."
  }
}
